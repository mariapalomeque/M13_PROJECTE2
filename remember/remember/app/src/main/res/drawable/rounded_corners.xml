<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo redondeado -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="360dp" />
        </shape>
    </item>
    <!-- Imagen -->
    <item android:drawable="@drawable/felipon" />
</layer-list>